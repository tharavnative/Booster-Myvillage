<div class="list-category row list_cat shopby_page layout__boxed default-page template-page">
  <div class="col-12"> 
    <div class="shopby_left">
    <header class="page-header">
      <h4 >Shop All Categories</h4>
  </header>
  <header class="page-header">
      <h5>Health Concern Categories</h5>
  </header>
      <style>
        .collection-title span{
          color:#006990;
        }

        .shopby_page.list_cat .collection-title-wrapper { padding: 0; }
.shopby_page.list_cat .collection-title-wrapper .collection-title h5 a { font-weight: 500; text-transform: capitalize;}
.shopby_page.list_cat .page-header h5 { letter-spacing: 0px; text-transform: capitalize; font-size: 20px; font-weight: 600; color: #61add0; margin-top: 50px;}

      </style>
{{ shop.url }}
  <div class="row">
  {% for collection in collections %} 
    {%- if collection.metafields.accentuate.health !='true' -%} 
        {%- continue -%}
    {%- endif -%}
    <div class="grid-item wow fadeIn col-3 {% if settings.list_collection_results_per_row == "2" %}col-sm-6{% elsif settings.list_collection_results_per_row == "3"%}col-sm-3{% elsif settings.list_collection_results_per_row == "3"%}col-sm-3{% else %}col{% endif%}" data-wow-delay="{{delayTime}}ms">
      <div class="background_section">
        <div class="product-top">
            {% comment %}
          <a href="{{ collection.url }}" title="{{ collection_title }}">
            {% if collection.image %}
            {{ collection.image.src | collection_img_url: 'large' | img_tag: collection_title }}
            {% else %}
            {{ collection.products.first.featured_image | product_img_url: 'large' | img_tag: collection_title }}
            {% endif %}
            <div class="hover"></div>
          </a>
          {% endcomment %}
          {% comment %}
          <a href="{{ collection.url }}" class="view">&nbsp;</a>
          {% endcomment %}
           
        
        <div class="collection-title-wrapper">
          <div class="collection-title">
            <h5 class="h5">
              <a href="{{ shop.url }}{{ '/' }}{{ collection.handle }}" title="{{ collection.title }}">
                  {{ collection.title }}
              </a>
            </h5>
            {% comment %}
            <p class="count-products" {% if settings.enable_multilang %}data-translate="collections.general.items_with_count|count:{{collection.all_products_count}}"{% endif %}>{{ 'collections.general.items_with_count' | t: count: collection.all_products_count }}</p>
            <p class="desc">{{ collection.description | strip_html | truncatewords: 14 }}</p>
        <p><a href="{{ collection.url }}" class="btn" {% if settings.enable_multilang %}data-translate="collections.general.shop_the_collection"{% endif %}>{{ 'collections.general.shop_the_collection' | t }}</a></p>
            {% endcomment %}
          </div>
        </div>
          </div>
        </div>
       </div>
    
      {% endfor %}
    </div>
      {% comment %}
      <div class="more_btn">
        <a href="https://myvillagegreen.myshopify.com/collections/">More Health Concerns</a>
      </div>
      {% endcomment %}
       
  </div>
 </div>
  <div class="col-12"> 
    <div class="shopby_right"> 
        <header class="page-header">
            <h5>Product Type Categories</h5>
        </header>
  <div class="row">
  {% for collection in collections %}
    {%- if collection.metafields.accentuate.product_type !='true' -%} 
        {%- continue -%}
    {%- endif -%}
    
    <div class="grid-item wow fadeIn col-3 {% if settings.list_collection_results_per_row == "2" %}col-sm-6{% elsif settings.list_collection_results_per_row == "3"%}col-sm-3{% elsif settings.list_collection_results_per_row == "3"%}col-sm-3{% else %}col{% endif%}" data-wow-delay="{{delayTime}}ms">
      <div class="background_section">
        <div class="product-top">
          {% comment %}
          <a href="{{ collection.url }}" title="{{ collection_title }}">
            {% if collection.image %}
            {{ collection.image.src | collection_img_url: 'large' | img_tag: collection_title }}
            {% else %}
            {{ collection.products.first.featured_image | product_img_url: 'large' | img_tag: collection_title }}
            {% endif %}
            <div class="hover"></div>
          </a>
          {% endcomment %}
          {% comment %}
          <a href="{{ collection.url }}" class="view">&nbsp;</a>
          {% endcomment %}
        
        <div class="collection-title-wrapper">
          <div class="collection-title">
            <h5 class="h5">
              <a href="{{ collection.url }}" title=" {{ collection.title }}" >
                  {{ collection.title }}
              </a>
            </h5>
            {% comment %}
            <p class="count-products" {% if settings.enable_multilang %}data-translate="collections.general.items_with_count|count:{{collection.all_products_count}}"{% endif %}>{{ 'collections.general.items_with_count' | t: count: collection.all_products_count }}</p>
            <p class="desc">{{ collection.description | strip_html | truncatewords: 14 }}</p>
        <p><a href="{{ collection.url }}" class="btn" {% if settings.enable_multilang %}data-translate="collections.general.shop_the_collection"{% endif %}>{{ 'collections.general.shop_the_collection' | t }}</a></p>
            {% endcomment %}
          </div>
        </div>
          </div>
        </div>
       </div>
  {% endfor %}
    </div>
  {% comment %}
      <div class="more_btn">
        <a href="https://myvillagegreen.myshopify.com/collections/">More Product Types</a>
      </div>
      {% endcomment %}
  </div>
 </div>
</div>

{% schema %}
  {
    "name": "Section name",
    "settings": []
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}